

  <h1 class = "large-top-space center"> Edit your response</h1>

    <div class = "primary-content app">

          <h3 class = "top-space center italic">
            "<%= @response.question.question_text %>"
          </h3>


  <%= render 'form' %>

      <p>or <%= link_to 'Cancel', question_responses_url(@question) %></p>

        <em><%= link_to 'Delete', question_response_url(@question, @response), method: 'delete', data: { confirm: "You sure about that?" } %></em>

    </div>
   </div>

