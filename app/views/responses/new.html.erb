<h1 class = "large-top-space center">Your response</h1>

  <div class = "primary-content ">

<!--     <h3 class = "top-space center italic">
      "<%= @response.question_id %>"
    </h3>
 -->

     <%= form_tag(questions_url, method: 'post') do%>

            <p>This is Week #
                <%= select_tag :week_id, options_from_collection_for_select(Week.all, :id, :week_number, {:selected => @question.week_id}) %>
            </p>

            <p>What question do you want to ask?
                <%= text_area_tag :question_text %>
            </p>



          <div class = "button-set">
            <%= submit_tag "Submit", :class => 'button' %>

        <% end %>

    <% end %>

  </div>


