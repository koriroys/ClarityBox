

    <h1 class = "large-top-space center">This week's question</h1>


  <div class = "primary-content app">

        <h4 class = "top-space">

          <time>
            (<%= @question.week.start_date.strftime("%b %e, %Y")  %> - <%= @question.week.end_date.strftime("%b %e, %Y")  %>)
          </time>
        </h4>


          <h3 class = "center italic">
            "<%= @question.question_text %>"
          </h3>

          <p><%= link_to 'Edit this question', edit_question_url(@question)%></p>

          <p><%= link_to 'Respond to this question', new_question_response_url(@question) %></p>

          <p><%= link_to 'See all the responses', question_responses_url(@question) %></p>

          <p>Ask this question on:  <%= @question.ask_at.strftime("%A, %b %e at %l:%M %p") %></p>
          <p>Send reminder on:  <%= @question.remind_at.strftime("%A, %b %e at %l:%M %p") %></p>
          <p>Send rollup on:  <%= @question.send_roll_up_at.strftime("%A, %b %e at %l:%M %p") %></p>

            <p><%= link_to 'Back to all the questions', questions_url %></p>

    </div>






