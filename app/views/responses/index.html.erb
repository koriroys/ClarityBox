


  <h1 class = "large-top-space center">Welcome to your ClarityBox.</h1>




  <div class = "primary-content center top-space">
    <%= link_to 'Respond to a question', weeks_url, :class => 'button'%>
  </div>

     <div class = "large-top-space">
    <p class = "center"><%= notice %></p>
    <% if session[:user_id].present? %>
     <p class = "center"> Welcome back,  <%= User.find(session[:user_id]).first_name %></p>

   </div>
    <% end %>
